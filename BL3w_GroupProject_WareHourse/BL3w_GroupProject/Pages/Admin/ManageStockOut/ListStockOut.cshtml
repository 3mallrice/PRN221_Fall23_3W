@page
@model BL3w_GroupProject.Pages.Admin.ManageStockOut.ListStockOutModel

@{
    ViewData["Title"] = "ListStockOut";
    Layout = "/Pages/Shared/_LayoutAdmin.cshtml";
}

<h1>ListStockOut</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<form method="post">
    <select name="SearchBy">
        <option value="AccountCode">Search by AccountCode</option>
        <option value="PartnerName">Search by PartnerName</option>
    </select>
    <input type="text" asp-for="Keyword" />
    <button type="submit" class="btn-primary">Search</button>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StockOut[0].DateOut)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StockOut[0].Note)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StockOut[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StockOut[0].Account.AccountCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StockOut[0].Partner.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.StockOut) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateOut)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                    @if (item.Status == 1)
                    {
                        <span class="text-success">Active</span>
                    }
                    else if (item.Status == 0)
                    {
                        <span class="text-danger">Inactive</span>
                    }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account.AccountCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Partner.Name)
            </td>
           
        </tr>
}
    </tbody>
</table>
<style>
    .horizontal-pagination {
        display: flex;
        justify-content: center;
    }

        .horizontal-pagination ul {
            list-style: none;
            display: flex;
            padding: 0;
            margin: 0;
        }

        .horizontal-pagination li {
            margin-right: 5px;
        }
</style>

<div class="horizontal-pagination">
    <ul>
        @for (var i = 1; i <= Model.TotalPages; i++)
        {
            <li class="@(i == Model.PageIndex ? "active" : "")">
                <a asp-page="./ListAccount" asp-route-pageIndex="@i">@i</a>
            </li>
        }
    </ul>
</div>